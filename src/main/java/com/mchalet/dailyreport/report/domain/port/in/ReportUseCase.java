package com.mchalet.dailyreport.report.domain.port.in;

/**
 * Inbound Port: Defines the contract for all use cases related to Reports.
 * Any external adapter (like a REST Controller or a Scheduled Job)
 * will depend on this interface, not on the concrete service implementation.
 */
public interface ReportUseCase {
//    Report createReport(CreateReportRequest request);

//    Page<Report> getAllReports(ReportFilter filter, Pageable pageable);

//    Report getReportById(ID reportId);

//    Report updateReport(ID reportId, UpdateReportRequest request);

//    void deleteReportById(ID reportId);
}